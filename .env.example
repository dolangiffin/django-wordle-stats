# ===========================================
# DJANGO SETTINGS
# ===========================================

# SECRET_KEY: Used for cryptographic signing (sessions, CSRF, etc.)
# For development: can use the default in settings.py
# For production: generate a unique key with:
#   python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-secret-key-here

# DEBUG: Shows detailed error pages. NEVER set True in production!
DEBUG=True

# ALLOWED_HOSTS: Which domains can serve this app
# For local dev: localhost,127.0.0.1
# For production: add your actual domain
ALLOWED_HOSTS=localhost,127.0.0.1

# ===========================================
# SUPABASE DATABASE CONNECTION
# Find these at: Supabase Dashboard > Project Settings > Database
# ===========================================

# Database name (usually 'postgres')
SUPABASE_DB_NAME=postgres

# Database user (usually 'postgres')
SUPABASE_DB_USER=postgres

# Your database password (you set this when creating the project)
SUPABASE_DB_PASSWORD=your-database-password-here

# Database host - Find under "Connection string" > "Host"
# Looks like: db.abcdefghijkl.supabase.co
SUPABASE_DB_HOST=db.xxxxxxxxxxxxx.supabase.co

# Database port (usually 5432, or 6543 for connection pooling)
SUPABASE_DB_PORT=5432

# ===========================================
# SUPABASE API (Optional - for future features)
# Find these at: Supabase Dashboard > Project Settings > API
# ===========================================

# Project URL
SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co

# anon/public key - safe to use in frontend
SUPABASE_ANON_KEY=your-anon-key-here

# service_role key - KEEP SECRET, only use server-side
SUPABASE_SERVICE_KEY=your-service-role-key-here